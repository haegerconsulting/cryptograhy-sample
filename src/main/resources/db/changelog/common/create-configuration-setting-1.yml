databaseChangeLog:
  - changeSet:
      id: create-cryptography_settings-1
      author: cenglisch
      changes:
        - createTable:
            tableName: cryptography_settings
            columns:
              - column:
                  name: id
                  type: varchar(255)
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: value
                  type: varchar(255)
